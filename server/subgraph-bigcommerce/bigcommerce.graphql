extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0",
        import: ["@key"])

type Query {
  "BigCommerce customer login"
  BigCommerceLogin (email: String!, pass: String!): result
}

type result {
  data: String
  errors: Errors
}

type Errors {
  message: String
}