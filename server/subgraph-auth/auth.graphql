extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0",
        import: ["@key"])
# do I need this since it's stateless? prob no?
type Query {
  "Returns all auth"
  auth: String!
}

type auth @key(fields: "id") {
  id: ID!,
  role: String!
}